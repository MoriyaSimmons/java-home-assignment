<div dir="rtl">

# DECISIONS

> מלאו את הקובץ הזה. הוא חלק מההערכה — קצר וברור עדיף על אר
- ..וך.

## 1. החלטות ארכיטקטוניות.
העברתי את הלוגיקה העסקית של בקשות החופשה מה-Controller ל-Service, כך שה-Controller נשאר אחראי על HTTP בלבד.
עבור אישור בקשות השתמשתי בטרנזקציה ובנעילה על רשומת העובד, מכיוון שמכסת החופשה היא משאב משותף בין מספר בקשות.
ב-Frontend השתמשתי ב-Reactive Forms כדי לרכז את חוקי הוולידציה במקום אחד ולשמור על הפרדה בין הקלט להצגת הנתונים.

## 2. הבאג ביתרת החופשה
- מה היה הבאג, איפה, ואיך תיקנתי:
 הבדיקה המקורית בLeaveRequestsController השוותה רק את מספר הימים בבקשה למכסה השנתית, ולכן לא התחשבה בימי חופשה שכבר אושרו לעובד. העברתי את הלוגיקה לLeaveRequestService ותיקנתי את הבדיקה כך שתתחשב גם בימים שכבר אושרו. בנוסף, פעולת היצירה מוגדרת כtransaction אחת.
הטסט שמוכיח את התיקון: create_ExceedsQuotaGivenExistingApprovedVacation_IsRejectedAndNotPersisted בLeaveRequestsTests. הטסט בודק שעובד עם מכסה של 20 ימים ו 18 ימים שכבר אושרו, שמגיש בקשה נוספת של 3 ימים, מקבל 400 Bad Request, את הודעת השגיאה המתאימה, ושהבקשה לא נשמרת.

## 3. אישור בקשה (approve) ו‑concurrency
- איך טיפלתי במצבים לא חוקיים (כבר אושר / לא קיים):
הוספתי POST /api/leave-requests/{id}/approve.
בקשה שלא קיימת מחזירה 404. בקשה שכבר אושרה או נדחתה מחזירה 409.
רק בקשות PENDING ניתנות לאישור.
לפני אישור חופשה נבדקת מחדש יתרת ימי החופשה.
כדי למנוע אישור מקביל של שתי בקשות שיגרום לחריגה מהמכסה, השתמשתי ב־@Transactional וב־PESSIMISTIC_WRITE על העובד. לאחר הנעילה מתבצע חישוב מחדש של הימים המאושרים.
הוספתי טסטים למקרי 404, 409, אישור תקין וחריגה מהמכסה.
- מה לגבי אישור של שתי בקשות במקביל:
שתי בקשות של אותו עובד ננעלות דרך רשומת העובד (PESSIMISTIC_WRITE), כך שאישור של בקשה אחת מסתיים לפני שהשנייה בודקת את היתרה. לכן לא ניתן לאשר במקביל בקשות שיחרגו מהמכסה
## 4. על מה ויתרתי בגלל הזמן
- ... ומה הייתי עושה עם עוד יום:
לא ביצעתי עדיין את ה-refactor המלא של ה-Frontend ל-Service ולטיפוסים חזקים.
לא הוספתי סינון לפי שנה למכסת החופשה.
לא הוספתי authentication ו-authorization, מאחר שזה מחוץ להיקף המשימה ודורש תכנון רחב יותר.
עם עוד יום הייתי מוסיף גם בדיקות אינטגרציה מלאות דרך HTTP, משפר את מודל ה-API עם DTOs ומוסיף טיפול מסודר יותר בשגיאות.

## 5. שימוש ב‑AI
### איפה AI עזר (כולל prompts)
prompt: "Review the leave quota logic and identify the bug." → עזר לזהות שהמכסה נבדקה מול הבקשה החדשה בלבד ולא מול ימי החופשה שכבר אושרו. תיקנתי את הלוגיקה והוספתי regression test למקרה הזה.

prompt: "How should concurrent leave approvals be handled when multiple requests share the same annual quota?" → העלה את הצורך לנעול את רשומת העובד בזמן האישור ולחשב מחדש את המכסה לאחר הנעילה. בדקתי את הפתרון מול מבנה הקוד ובחרתי להשתמש ב-PESSIMISTIC_WRITE.

### איפה דחיתי/תיקנתי הצעה של AI
- מה AI הציע, למה זה היה שגוי, ומה עשיתי במקום:
AI הציע להשתמש ב-@Version על LeaveRequest כדי לפתור את בעיית ה-concurrency. דחיתי את הפתרון כי המכסה משותפת לעובד, ו-@Version על שתי בקשות שונות לא מונע את אישורן במקביל. במקום זאת השתמשתי ב-PESSIMISTIC_WRITE על רשומת העובד

### אבטחה
- אם מצאתם בעיית אבטחה: מה מצאתם, איפה (קובץ + שורה), למה זו בעיה, ואיך תיקנתם:

## 6. הוראות הרצה
- (אם שיניתם משהו מהוראות ה‑README המקורי)

</div>
